const facts=[['science','A day on Venus is longer than a year on Venus.'],['science','Octopuses have three hearts, and two of them stop beating when they swim.'],['science','Bananas are berries, but strawberries are not berries botanically.'],['science','Sound travels about four times faster through water than through air.'],['nature','Cows have best friends and can become stressed when separated from them.'],['nature','A group of flamingos is called a flamboyance.'],['nature','Sea otters hold hands while sleeping so they do not drift apart.'],['nature','Some bamboo species can grow more than three feet in a single day.'],['history','The first computer mouse was made of wood.'],['history','The shortest war in history lasted less than an hour.'],['history','The ancient Romans used crushed shells and charcoal to clean their teeth.'],['history','The first Olympic games included a race while wearing armor.']];let currentCategory='all';let currentFact;const factElement=document.getElementById('fact');const categoryElement=document.getElementById('category');function showFact(){const choices=facts.filter((fact)=>currentCategory==='all'||fact[0]===currentCategory);let next=choices[Math.floor(Math.random()*choices.length)];if(choices.length>1)while(next===currentFact)next=choices[Math.floor(Math.random()*choices.length)];currentFact=next;categoryElement.textContent=next[0];factElement.textContent=next[1];document.getElementById('fact-number').textContent=`Fact ${facts.indexOf(next)+1}`;}
document.getElementById('new-fact').addEventListener('click',showFact);document.querySelectorAll('.category').forEach((button)=>button.addEventListener('click',()=>{currentCategory=button.dataset.category;document.querySelectorAll('.category').forEach((item)=>item.classList.toggle('active',item===button));showFact()}));showFact();
